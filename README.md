# Blogify Module 2 Architecture
this was my project

Features:
Modular routing
Controller pattern
Versioned API
Standard responses

Run:
npm install
npm run dev

Endpoints:
GET /api/v1/posts
GET /api/v1/posts/:postId
